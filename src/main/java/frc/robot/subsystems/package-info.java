/**
 * This package contains all the subsystems that a robot has, such as the drivebase, scoring
 * mechanisms, cameras, etc.
 *
 * <p>Subsystems are in their own folders, and any additional files that a Subsystem needs to run,
 * such as IO interface layers, are put in the same folder as the Subsystem.
 */
package frc.robot.subsystems;
